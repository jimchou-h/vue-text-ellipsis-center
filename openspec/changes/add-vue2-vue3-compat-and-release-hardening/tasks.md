## 1. 发布结构收口

- [x] 1.1 选定唯一发布来源（根包或子包）并统一 package 元数据来源
- [x] 1.2 对齐 `main/module/exports/types/files` 到真实构建产物路径
- [x] 1.3 修复声明文件入口命名与路径错误并验证 `npm pack` 内容

## 2. 双版本运行时兼容

- [x] 2.1 引入并配置 `vue-demi` 兼容层及 Vue 2 侧依赖声明
- [x] 2.2 明确并落地 Vue 2 支持基线：同时覆盖 `2.6 + @vue/composition-api` 与 `2.7`
- [x] 2.3 将核心渲染实现切换为中性 render 方案（`h`/render 优先，非 TSX 绑定）
- [x] 2.4 将核心运行时 API 抽象为跨版本可用实现（插件安装、组件注册、响应式与生命周期）
- [x] 2.5 将 VueUse 从核心运行时路径移除或降级为可替换实现，补齐本地工具封装

## 3. 测试矩阵与质量门禁

- [x] 3.1 重构易碎白盒测试为面向用户行为的黑盒测试
- [x] 3.2 建立版本矩阵并跑通：Vue `2.6 + composition-api`、Vue `2.7`、Vue `3.x`
- [x] 3.3 为关键行为增加跨版本一致性断言（省略方向、行数、展开收起、插槽顺序）
- [x] 3.4 增加发布前阻断检查（构建、测试、元数据一致性）并接入 CI

## 4. 示例与文档对齐

- [x] 4.1 提供最小可运行的 Vue `2.6 + composition-api` 示例并完成 smoke 验证
- [x] 4.2 提供最小可运行的 Vue `2.7` 示例并完成 smoke 验证
- [x] 4.3 提供最小可运行的 Vue `3.x` 示例并完成 smoke 验证
- [x] 4.4 更新 README：支持矩阵、安装说明、迁移说明（TSX 到中性 render）与发布说明
